{
  "build": {
    "dockerfile": "Dockerfile",
    "context": "."
  },
  "install": "python3 -m pip install -e '.[dev]'",
  "start": "python3 -m racedna serve --host 0.0.0.0 --port 8000",
  "terminals": [
    {
      "name": "RaceDNA",
      "command": "python3 -m racedna serve --host 0.0.0.0 --port 8000"
    }
  ]
}
